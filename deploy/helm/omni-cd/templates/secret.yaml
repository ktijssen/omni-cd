{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "omni-cd.fullname" . }}
  labels:
    {{- include "omni-cd.labels" . | nindent 4 }}
type: Opaque
stringData:
  OMNI_ENDPOINT: {{ .Values.omni.endpoint | quote }}
  OMNI_SERVICE_ACCOUNT_KEY: {{ .Values.omni.serviceAccountKey | quote }}
  ADMIN_PASSWORD: {{ .Values.config.adminPassword | quote }}
  {{- if .Values.config.webhookSecret }}
  WEBHOOK_SECRET: {{ .Values.config.webhookSecret | quote }}
  {{- end }}
  {{- if .Values.oidc.enabled }}
  OIDC_CLIENT_SECRET: {{ .Values.oidc.clientSecret | quote }}
  {{- end }}
{{- end }}
